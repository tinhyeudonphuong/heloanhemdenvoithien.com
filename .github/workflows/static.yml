# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Single deploy job since we're just deploying
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>AI NHÌN LÀ GAY - Cày Thuê Blox Fruits</title>

<style>
*{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family:Arial,Helvetica,sans-serif;
}

body{
    background:#0f172a;
    color:white;
}

header{
    background:linear-gradient(90deg,#ff8a00,#ff0058);
    padding:25px;
    text-align:center;
}

header h1{
    font-size:40px;
}

header p{
    margin-top:10px;
}

.author{
    margin-top:15px;
    font-size:18px;
    font-weight:bold;
    color:#fff;
}

.container{
    width:90%;
    max-width:1200px;
    margin:auto;
}

.services{
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(250px,1fr));
    gap:20px;
    margin-top:30px;
}

.card{
    background:#1e293b;
    padding:20px;
    border-radius:15px;
    transition:.3s;
}

.card:hover{
    transform:translateY(-8px);
    box-shadow:0 0 20px #00d9ff;
}

.card h2{
    color:#00d9ff;
    margin-bottom:15px;
}

.card ul{
    list-style:none;
}

.card li{
    margin:8px 0;
}

.contact{
    text-align:center;
    margin:50px 0;
}

button{
    background:#00d9ff;
    color:white;
    border:none;
    padding:15px 35px;
    border-radius:30px;
    font-size:18px;
    cursor:pointer;
    transition:.3s;
}

button:hover{
    background:#0099cc;
}

footer{
    background:#111827;
    text-align:center;
    padding:20px;
    margin-top:40px;
}
</style>
</head>

<body>

<header>
    <h1>🔥 AI NHÌN LÀ GAY 🔥</h1>
    <p>Dịch vụ Cày Thuê Blox Fruits Uy Tín</p>
    <div class="author">
        👤 Người sáng tác: Gấu Là Gay
    </div>
</header>

<div class="container">

<div class="services">

<div class="card">
    <h2>⚔️ Cày Level</h2>
    <ul>
        <li>Lv 1 → Max</li>
        <li>Nhanh - Uy tín</li>
        <li>Giá từ 100K</li>
    </ul>
</div>

<div class="card">
    <h2>🍎 Raid</h2>
    <ul>
        <li>Normal Raid</li>
        <li>Dough Raid</li>
        <li>Fragment Farm</li>
    </ul>
</div>

<div class="card">
    <h2>👑 Race</h2>
    <ul>
        <li>V2</li>
        <li>V3</li>
        <li>V4</li>
    </ul>
</div>

<div class="card">
    <h2>🥋 Fighting Style</h2>
    <ul>
        <li>Godhuman</li>
        <li>Sanguine Art</li>
        <li>Electric Claw</li>
    </ul>
</div>

<div class="card">
    <h2>🗡️ Kiếm</h2>
    <ul>
        <li>CDK</li>
        <li>Shark Anchor</li>
        <li>TTK</li>
    </ul>
</div>

<div class="card">
    <h2>🐉 Sea Event</h2>
    <ul>
        <li>Leviathan</li>
        <li>Terrorshark</li>
        <li>Frozen Dimension</li>
    </ul>
</div>

</div>

<div class="contact">

    <h2> Thiên không có người yêu gay rồi haha🤣</h2>

    <br>

    <button onclick="alert('Thiên ơi cố lên nhé 😂')">
        Đặt Dịch Vụ
    </button>

</div>

</div>

<footer>
    | Blox Fruits Services
</footer>

</body>
</html>
